export interface ScheduleDefinition {
    id: string;
    prompt: string;
    cron: string;
    mode: "repeat" | "once";
    runAt?: string;
    enabled: boolean;
    createdAt: string;
    lastRunAt?: string;
    lastResult?: string;
}
export declare function discoverSchedules(agentDir: string): Promise<ScheduleDefinition[]>;
export declare function loadSchedule(filePath: string): Promise<ScheduleDefinition>;
export declare function saveSchedule(agentDir: string, schedule: ScheduleDefinition): Promise<string>;
export declare function deleteSchedule(agentDir: string, id: string): Promise<void>;
export declare function updateScheduleMeta(agentDir: string, id: string, updates: Partial<Pick<ScheduleDefinition, "lastRunAt" | "lastResult" | "enabled">>): Promise<void>;
